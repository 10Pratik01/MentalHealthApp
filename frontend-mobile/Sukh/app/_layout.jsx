import { Stack } from "expo-router";
import './global.css';

export default function RootLayout() {
  return <Stack>
   
    {/* <Stack.Screen 
      name="(tabs)"  ---> folder name 
      options={{ headerShown: false }} -- options on the folder
    />
    <Stack.Screen 
      name="movies/[id]"
      options={{ headerShown: false }}
    /> */}


  </Stack>;
}
